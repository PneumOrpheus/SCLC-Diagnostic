# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# Environments
venv/
.venv/
anaconda3/

# Model weights and large local data
*.pth
NIFTI_data/

# Everything under results/ is regenerable from checkpoints + scripts.
# Tracked files already in git stay tracked; this only blocks new additions.
/results/*

# Ad-hoc reference uploads at repo root (uploaded papers, dataset manifests, etc.).
# Root-anchored so the tracked thesis PDFs under results/figures/ stay tracked.
/*.pdf
/*.xlsx

# Internal-only docs
CLAUDE.md
.claude/
